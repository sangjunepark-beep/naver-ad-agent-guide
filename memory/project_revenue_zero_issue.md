---
name: 매출 0원 이슈 — 원인 규명 + A안 핫픽스 적용 완료 (2026-04-20)
description: 원인은 01시트에 상품ID 컬럼 부재 → 시트_저장_Phase2 output에 상품ID 누락 → 데이터_합산 JOIN 실패. A안(상품명 fallback) 적용으로 매출 복구. B안 구조 정식화 대기
type: project
originSessionId: bb2cc500-121b-4ea9-8757-74ff04ebb61c
---

## 2026-04-20 진단 결과 (확정)

**근본 원인**: 01_광고집계 시트 구조(A~K 11열)에 **상품ID 컬럼 부재** → `시트_저장_Phase2` output의 keys에 `상품ID` 없음 → `데이터_합산` 노드에서 `r['상품ID']` = undefined → `convMap[""]` 조회 실패 → 전 소재 매출 0원.

**A안 핫픽스 적용 완료 (exec 163 검증)**
- 수정 노드: `데이터_합산` (메인 파이프라인 UJrNqijTudgU91sX)
- 추가 로직: `상품매핑_읽기`에서 `상품명 → adId` 역매핑(`nameToAdId`) 빌드 + merged 매핑 시 `adId = r['상품ID'] || nameToAdId[r['상품명']] || ''` fallback
- 검증 결과 (04-19 데이터): 상품ID 1270/1270 채움 / 전환매출 >0 items 8건 / 총 매출 301,500원 / 실 ROAS 42.8%

## 미결

1. **B안 구조 정식화 (Task #8)** — 01 시트에 상품ID 컬럼 정식 추가 + 수집 노드 수정 + `시트_저장_Phase2` schema 업데이트. A안 핫픽스에 의존하지 않는 본질 수정.
2. **03_실_ROAS_대시보드 04-14 멈춤 (Task #9)** — 별건. 대시보드 자체 QUERY 수식·날짜 참조 깨짐. 리포트 매출에는 영향 없음(리포트는 대시보드 참조 안함).

## 백업 파일
- `/sessions/kind-charming-goldberg/mnt/단순등록자동화/wf_backup_20260420_A안적용전.json`

**How to apply**: A안은 `데이터_합산` 코드 한 노드 안의 핫픽스라 상품매핑 시트에 상품명 중복이 있을 경우 첫 매칭 우선으로 오매핑 가능(현재 동명이품 거의 없어 실용 OK). B안 정식화 전까지 한시적 운용.

---
## (참고) 진단 전 기록

**증상 (04-19자 리포트, 04-20 수신)**
- 비용: 704,279원 / 노출 204,788 / 클릭 505 / CTR 0.25%
- **전 캠페인 매출 0원 / 실 ROAS 0% / 전부 '경고'**
- AI 교차 검증: 자동실행 0 / 승인필요 0 / 보류 0
- Verifier_L2 응답: "하나사인몰 네이버 쇼핑검색광고 운영 데이터 부재로 검증 불가"

**정상 작동 부분**
- Phase 2-B 전환 수집 정상 (`Conv_TSV_파싱` items=9, `시트_저장_Conv` items=9 실행 이력 확인)
- 파이프라인 E2E 완주 (60노드 성공, 메일 발송 성공)

**추정 원인 후보**
1. `03_실_ROAS_대시보드`의 매출 집계 수식 참조 컬럼 오류 (구매전환매출 vs 총전환매출)
2. 상품매핑 JOIN 키 불일치로 매출 0원 채워짐
3. 리포트 생성 코드에서 `_meta.캠페인별` 집계 시 매출 필드 매핑 실패
4. Verifier_L2 프롬프트에 실제 지표 미전달 (별개 이슈지만 증상 연관)

**How to apply**: 보고서 개편 착수 전에 이 데이터 경로부터 복구해야 리포트 내용이 의미를 가짐. 진단 순서 — 02_전환수집로그 원시 데이터 → 상품매핑 JOIN → 03_대시보드 계산 → 리포트_생성 코드의 `_meta.캠페인별` 집계 로직 → Verifier_L2 프롬프트 입력값.

**관련 메모**: `project_naver_ad_conversion_inflation.md` (총 전환매출 5배 부풀림 이슈 - 실매출은 `구매전환매출` 컬럼 사용 원칙)
