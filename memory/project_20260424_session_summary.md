---
name: 2026-04-24 세션 종합 — 날짜 버그·채널 분류·일자 보정·허수 검증
description: 하루 만에 n8n 파이프라인의 5개 날짜 버그, 채널 분류(영호/하나몰) 버그, 일자 오타 자동 보정 추가. 네이버 광고 매출 부풀림은 공식문서·실데이터 대조 후 1.3~1.9배(중복 아님). Claude in Chrome 유령 다운로드 원인 특정
type: project
originSessionId: 35d08f6a-12fd-40ba-9cd3-904848f3b709
---
**사실**: 2026-04-24 세션에서 n8n 워크플로우 `UJrNqijTudgU91sX`(네이버 검색광고 에이전트_파이프라인)에 총 **3회 PUT** 수행:

1. **5개 노드 KST 날짜 버그 패치** (PUT 06:30 UTC)
   - 서명_생성_stats_POST / 서명_생성_conv_POST / 서명_생성_stats_POST_B: UTC `new Date()` → `Date.now() + 9h` 보정
   - 채널별_집계 / 광고비교_집계: `shift()` 함수가 KST 자정을 UTC로 포매팅해서 Tm1/Tm2가 하루씩 앞으로 밀리던 버그 → `Date.UTC()` 기반 일관 포매팅
2. **채널별_집계 mapChannel 분류 버그 패치** (PUT 06:37 UTC)
   - `사인몰(영호)(X)` 복합 진행자가 CS로 잘못 분류되던 것 → `includes('(영호)')` 전부 영업
   - `하나몰(X)`이 '기타'로 빠지던 것 → `startsWith('사인몰')||startsWith('하나몰')` 전부 CS
3. **일자 보정 로직 추가** (PUT 07:02 UTC)
   - 이웃 ±2행 중 3개 이상이 동일 일자인데 현재 행만 다르면 → 주변 일자로 보정
   - 전체 시트 기준 18건 오타 발견(2028-03-31 같은 타이포 포함). 04-23 CS 집계 결과 보정 후 22건/6,033,460원 (보정 전 21건/5,804,460원)

또한 **OOM 방지 settings 변경**: `saveExecutionProgress=false, saveManualExecutions=false, saveDataSuccessExecution=none`. 수동 실행 시 중간 데이터 미저장으로 메모리 확보. 실행 자체는 정상(메일·GitHub Pages 액션 수행).

**네이버 광고 매출 부풀림 재검증**: 공식문서 교차조사 + exec224 원본 Conv TSV 분석 결과:
- Conv API는 `convType`으로 purchase/add_to_cart 명확 분리 (04-23 purchase 13건 1,087,500원 / cart 22건 3,439,910원)
- 같은 소재에 purchase 중복 행 0개 — 공식문서 정의대로 동작
- 기간 누적 대조 시 실결제 대비 1.33~1.86배 부풀림은 존재하지만, "장바구니 가치 합산" 때문 아님
- 원인 후보: 상품 단위 매출 합산, 취소/환불 미반영, 간접전환 20일 창 멀티터치
- 따라서 **04-14 메모의 "5배 부풀림"은 과장이었음. 현실은 1.3~1.9배 수준**

**Claude in Chrome 유령 다운로드 원인 특정**: Downloads에 `data (N).csv`, `json (N).txt` 반복 생성 → 브라우저 상단 "`Claude`에서 이 브라우저에 대한 디버깅을 시작함" 배너 + "Review Google Sheets" 탭 확인 → 사무실 PC에서 시작한 Cowork 세션이 Claude 확장 통해 현재 PC 브라우저를 제어 중이던 것. 사무실 PC 크롬 로그아웃+세션 전체 삭제로 근본 차단.

**Why**: 04-23 리포트의 "영업 매출 0원"과 "CS 9,629,910원" 표시가 실제 업체계약현황 시트와 불일치한다는 차장님 지적으로 시작된 전수 점검. 검증 과정에서 연쇄로 5개 날짜 버그, 채널 분류 2버그, 일자 오타 다수 발견. 네이버 매출 부풀림 주장도 재검증하여 과거 메모 일부 정정 필요.

**How to apply**:
- 리포트는 내일 09:00 스케줄부터 정상 분류로 표시됨(영업/CS/하나몰 분리, 일자 오타 자동 보정)
- 과거 리포트 소급 재생성은 별도 작업 필요
- `project_naver_ad_conversion_inflation.md`(04-14 5배 부풀림 주장) 과거 메모는 **과장**. 현실은 1.3~1.9배. 다만 여전히 실결제 대조·자체 집계 로직 필요성은 유효
- 리포트에 "보정된 건수 N건" 섹션을 추가 표시하면 감사 용이 (현재 `_날짜보정_건수`, `_날짜보정_로그` 필드로 전달됨. 리포트_생성 노드에서 참조만 추가하면 됨)
- n8n 수동 실행 시 OOM 나면 이번 settings 변경으로 거의 해결. 그래도 OOM이면 스케줄 cron을 현재+2분으로 임시 변경 → 자동 트리거는 UI 수동보다 메모리 적게 씀

**주의·재발 방지**:
- PUT 3회 연속 수행할 때 **최신 GET 기반 누적 수정 후 1회 PUT**이 원칙이었으나 이번은 각 수정 직후 바로 PUT함. 세션 내 다른 변경자 없었기 때문에 문제 없었음. 팀원이 동시 수정하는 환경이었다면 덮어쓰기 위험.
- 날짜 버그는 `new Date()` + `getDate()/getFullYear()` 같은 **로컬 TZ 의존 메서드**가 n8n cloud(UTC 컨테이너)에서 쓰이면 자주 발생. 앞으로 새 JS 노드 추가 시 `getUTC*` / `Date.UTC()` 사용 원칙.
- 채널 분류 규칙은 `includes('(영호)')` + `startsWith('사인몰')||startsWith('하나몰')` 조합. 신규 진행자 유형 추가 시 mapChannel 업데이트 필요.
